# Daily DSA Practice – Day 04 (Array Based Questions)

This section contains important **array-based DSA questions** that are commonly asked in technical interviews.  
The goal is to strengthen problem-solving skills and build a strong foundation in arrays.

## Questions

1. Remove duplicate elements from an array  
2. Find the largest and smallest element in an array  
3. Find the second largest element in an array  
4. Reverse an array without using the built-in `reverse()` method  
5. Check whether an array is sorted or not  
6. Find the missing number in a given sequence  
7. Move all zeroes to the end of the array  
8. Count the frequency of each element in an array  
9. Find duplicate elements in an array  
10. Merge two arrays without duplicates  
11. Find common elements between two arrays  
12. Rotate an array by **K** positions  
13. Find the first non-repeating element in an array  
14. Check if two arrays are equal  
15. Sort an array without using the built-in `sort()` method  

---

📌 *These questions are part of daily DSA practice and are focused only on array concepts for interview preparation.*
